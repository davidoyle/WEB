import PageShell from '../components/PageShell';
import Landing from '../sections/Landing';

const HomePage = () => (
  <PageShell
    title="Workers Toolkit"
    description="Built by an injured worker. For every worker they're betting will quit."
    mainClassName=""
  >
    <Landing />
  </PageShell>
);

export default HomePage;
